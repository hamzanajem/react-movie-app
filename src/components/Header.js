import MoviesGrid from "./MoviesGrid";

export default function Header() {
    return (
        <div className="header">
            <img src="logo.png" alt="moviedux" />
            <h2 className="app-subtitle">It's time for popcorn! find your next movie here</h2>

        </div>
    );
}